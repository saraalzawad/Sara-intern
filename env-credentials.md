# Environment Variables Reflection

Using a `.env` file is more secure because it keeps database usernames and passwords out of the Python code. This helps protect sensitive information if the project is shared on GitHub.

The `python-dotenv` package makes it easy to load environment variables from the `.env` file into a Jupyter Notebook. It helps keep the code clean and makes it easier to manage database credentials.
